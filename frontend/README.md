# Frontend Placeholder

This is a minimal static frontend placeholder demonstrating how to call the API.

Open `frontend/index.html` in your browser (or serve it) and click "Load Customers" to fetch the customers list from the API running at `http://127.0.0.1:8000`.
